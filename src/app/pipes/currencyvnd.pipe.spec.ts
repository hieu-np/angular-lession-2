import { CurrencyvndPipe } from './currencyvnd.pipe';

describe('CurrencyvndPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyvndPipe();
    expect(pipe).toBeTruthy();
  });
});
